import { LoadingIconAnimation } from '../components/Icons';

export default function Loading() {
  return (
    <div className="flex items-center justify-center h-screen">
      <LoadingIconAnimation />
    </div>
  );
}
